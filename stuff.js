var counter=function(arr){
    return 'There are '+ arr.length+ ' elements'
}

var adder=function(a,b){
    return `The some of the 2 numbers is ${a+b}`
}

var pi=3.142
module.exports={
    counter,
    adder,
    pi
}